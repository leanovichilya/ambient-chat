AMBIENT_API_KEY=key
AMBIENT_BASE_URL=https://api.ambient.xyz/v1
AMBIENT_MODEL=zai-org/GLM-4.6
AMBIENT_CONNECT_TIMEOUT=10
AMBIENT_READ_TIMEOUT=180

# live / collected
STREAM_MODE=live
SHOW_REASONING=1

# Optional proxies (requests/httpx will use these if set in different ways):
# proxy off / on - 0 / 1
USE_PROXY=0
HTTP_PROXY=http://127.0.0.1:8888
HTTPS_PROXY=http://127.0.0.1:8888
